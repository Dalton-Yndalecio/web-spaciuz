---
import ButtonComponent from '../ui/ButtonComponent.astro';

const links = [
  {
    text: 'Become a Host',
    url: '/become-host',
  },
  {
    text: 'Beneficios',
    url: '#beneficios',
  },
  {
    text: 'Become a Host',
    url: '/become-host',
  },
];
---

<!-- bg-transparent  backdrop-blur-xl fixed z-50 shadow-2xl -->
<header class="hidden lg:flex h-[61px] w-full px-[10%] py-[50px] ">
  <div class="flex items-center justify-start w-1/3">
    <img
      src="/public/images/logos/logo-spaciuz.png"
      alt="Logo Spaciuz"
      class="h-[61px]"
    />
  </div>
  <div class="flex items-center justify-end w-2/3 gap-24">
    <div class="items-center justify-center space-x-10">
      {
        links.map(({ text, url }) => (
          <a
            href={url}
            class="text-[20px] font-bold cursor-pointer hover:text-[#8059FF]"
          >
            {text}
          </a>
        ))
      }
    </div>
    <ButtonComponent text="Regístrate" url="#formulario" />
  </div>
</header>
